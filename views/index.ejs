<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chess Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .chessboard {
            display: grid;
            grid-template-columns: repeat(8, minmax(0, 1fr));
            grid-template-rows: repeat(8, minmax(0, 1fr));
            width: 400px;
            height: 400px;
            transform: rotate(0deg);
        }

        .piece {
            font-size: 36px;
            cursor: pointer;
            z-index: 10;
        }

        .piece.white {
            color: white;
            filter: drop-shadow(1px 1px 1px rgba(0,0,0,0.5));
        }

        .piece.black {
            color: black;
        }

        .piece.draggable { cursor: grab; }
        .dragging { opacity: 0.5; }

        .square {
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative; 
        }

        .square.light { background-color: #eeeed2; }
        .square.dark { background-color: #769656; }

        /* --- VISUAL HINT (Yellow Highlight) --- */
        .highlight-hint {
            background-color: rgba(255, 235, 59, 0.7) !important;
            box-shadow: inset 0 0 5px rgba(255, 193, 7, 0.8);
        }

        .coordinate {
            position: absolute;
            font-size: 10px;
            font-weight: bold;
            pointer-events: none;
            user-select: none;
        }
        .coordinate.rank { top: 2px; left: 2px; }
        .coordinate.file { bottom: 2px; right: 2px; }
        
        .square.light .coordinate.rank { color: #769656; }
        .square.dark .coordinate.rank  { color: #eeeed2; }
        .square.light .coordinate.file { color: #769656; }
        .square.dark .coordinate.file  { color: #eeeed2; }

        .flipped { transform: rotate(180deg); }
        .flipped .piece { transform: rotate(180deg); }
        .flipped .coordinate { transform: rotate(180deg); }

        .captured-piece {
            font-size: 28px;
            margin-right: 2px;
            color: red;
            filter: drop-shadow(1px 1px 1px rgba(0, 0, 0, 0.3)); 
        }
    </style>
</head>
<body class="bg-slate-800 text-white h-screen flex flex-col items-center justify-center font-sans gap-6 relative">

    <div id="controls-black" class="w-96 flex justify-between items-center bg-white/10 p-4 rounded-lg shadow-lg backdrop-blur-sm z-10 relative">
        <div class="flex items-center gap-3">
            <span class="font-bold text-lg text-gray-200">Black</span>
            <div id="captured-white" class="flex text-gray-300 items-center"></div> 
        </div>
        <div id="timer-black" class="bg-gray-700 px-3 py-1 rounded font-mono text-xl">10:00</div>
    </div>

    <div class="chessboard w-96 h-96 rounded-lg shadow-2xl overflow-hidden z-0"></div>

    <div id="controls-white" class="w-96 flex justify-between items-center bg-white/10 p-4 rounded-lg shadow-lg backdrop-blur-sm relative z-10">
        <div class="flex items-center gap-3">
            <span class="font-bold text-lg text-gray-200">White</span>
            <div id="captured-black" class="flex text-gray-900 items-center"></div>
        </div>
        <div id="timer-white" class="bg-gray-700 px-3 py-1 rounded font-mono text-xl">10:00</div>
    </div>

    <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
    <script src="/js/chessgame.js"></script>
</body>
</html>